@model IEnumerable<SacramentMeetingPlanner.Models.Assignment>

@{
    ViewData["Title"] = "Assignments";
}

<h1>Sacrament Assignments</h1>

<p>
    <a asp-action="Create" class="button">Create New Assignment</a>
</p>

<form asp-action="Index" method="get">
    <div class="form-actions no-color">
        <p>
            Search: <input type="text" name="SearchString" value="@ViewData["currentFilter"]" />
            <input type="submit" value="Search" class="btn btn-default" /> |
            <a asp-action="Index" class="no-btn">Back to Full List</a>
        </p>
    </div>
</form>

<table class="table">
    <thead>
        <tr>
            <th>
                <a asp-action="Index" asp-route-sortOrder="@ViewData["NameSortParm"]" class="no-btn">@Html.DisplayNameFor(model => model.MemberID)<img src="~/images/sort-solid.svg" alt="Sort" style="width:20px;height:15px;" /></a>
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MeetingID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.assignment)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Topic)
            </th>
            <th>
                Actions
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MemberID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MeetingID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.assignment)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Topic)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID"><img src="~/images/edit-regular.svg" alt="Edit" style="width:20px;height:15px;" /></a> |
                <a asp-action="Details" asp-route-id="@item.ID"><img src="~/images/eye-solid.svg" alt="Details" style="width:20px;height:15px;" /></a> |
                <a asp-action="Delete" asp-route-id="@item.ID"><img src="~/images/trash-solid.svg" alt="Delete" style="width:20px;height:15px;" /></a>
            </td>
        </tr>
}
    </tbody>
</table>
